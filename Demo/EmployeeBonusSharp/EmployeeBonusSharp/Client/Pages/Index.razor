@page "/"
@inject HttpClient Http

<h1>Bonus Calculator</h1>
<br />


@if (Employees is not null)
{
    <table class="table" style="width:1000px">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Date Of Employment</th>
                <th>Certificates</th>
                <th>Bonus points</th>
                <th>Bonus (@BonusCurrencyName)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Employee employee in Employees)
            {
                <tr>
                    <td>@employee.FirstName</td>
                    <td>@employee.LastName</td>
                    <td>@employee.DateOfEmployment</td>
                    <td>@employee.Certificates?.Count</td>
                    <td>@employee.EmployeeBonus?.TotalPoints</td>
                    <td>@employee.EmployeeBonus?.TotalMoney.Amount</td>
                </tr>
            }
        </tbody>
    </table>

    <br />

    <EditForm Model="this">
        <span>
            1 bonus point =
            <InputNumber @bind-Value="Company.BonusPointToMoney" style="width:75px" />
            @BonusCurrencyName
        </span>
    </EditForm>

    <br />

    <button class="btn btn-primary" @onclick="RecalculateBonus">Recalculate bonus</button>
}
